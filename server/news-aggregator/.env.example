# Куда складывать результат прогона (JSON со свежими DRAFT-кандидатами)
OUTPUT_DIR=./output

# Необязательно: если бэкенд BROK уже поднят, можно пушить кандидатов сразу
# в него вместо файла (не реализовано в этой заготовке — см. SPEC.md, пункт
# "Что нужно сделать перед запуском в проде").
# NEWS_API_URL=
